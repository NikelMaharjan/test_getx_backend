

import 'package:flutter/material.dart';

class SecondScrenn extends StatelessWidget {
  const SecondScrenn({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Nikel Maharajn"),
      ),
    );
  }
}
